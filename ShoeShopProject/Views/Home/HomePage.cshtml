<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>Dashboard - SB Admin Pro</title>
    <link href="https://cdn.jsdelivr.net/npm/simple-datatables@latest/dist/style.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/litepicker/dist/css/litepicker.css" rel="stylesheet" />
    <link href="/css/styles.css" rel="stylesheet" />
    <link rel="icon" type="image/x-icon" href="/assets/img/favicon.png" />
    <script data-search-pseudo-elements defer src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/js/all.min.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/feather-icons/4.28.0/feather.min.js" crossorigin="anonymous"></script>
</head>
<body class="nav-fixed sidenav-toggled">
    @Html.Partial("_Header")
    <!--Content-->
    <div id="layoutSidenav">
        <div id="layoutSidenav_content">
            <main>
                <header class="mb-4">
                    <div class="banner">
                        <div class="slider">
@*                             @foreach (CommonMaster banner in bannerList)
                            {
                                <a href="@Url.Action("ViewProgramDetails", "Programs", new {programId = banner.Description})"><img src="@banner.MasterName" alt="@banner.MasterType"></a>
                            }
                            @if (bannerList[0] is CommonMaster bannerFirst)
                            {
                                <a href="@Url.Action("ViewProgramDetails", "Programs", new {programId = bannerFirst.Description})"><img src="@bannerFirst.MasterName" alt="@bannerFirst.MasterType"></a>
                            } *@

                            <a href="#"><img src="/assets/img/slider/banner-tet.png" alt=""></a>
                            <a href="#"><img src="/assets/img/slider/sale-cuoimua.jpg" alt=""></a>
                            <a href="#"><img src="/assets/img/slider/banner-tet.png" alt=""></a>
                        </div>
                        <button class="prev" onclick="prevSlide()">&#10094;</button>
                        <button class="next" onclick="nextSlide()">&#10095;</button>
                    </div>
                </header>
                <!-- Search Bar -->
                <div class="row mb-4 d-flex justify-content-center">
                    <div class="col-sm-5 me-2">
                        <input type="text" class="form-control me-2" id="search-bar" placeholder="Tìm kiếm tên sản phẩm, thương hiệu,...">
                    </div>
                    <div class="col-sm-1 ms-2">
                        <button type="button" class="btn btn-primary">Tìm kiếm</button>
                    </div>
                </div>
                <!-- Main page content-->
                <div class="row px-10">
                    <div class="col-lg-2">
                        <!-- Filter by Brand -->
                        <div class="mb-4">
                            <h5>Thương hiệu</h5>
                            <div class="form-check card-scrollable">
                                <div class="row">
                                    @for (int i = 0; i < 20; i++)
                                    {
                                    <div class="col-sm-3 mx-2">
                                        <input class="form-check-input" type="checkbox" value="" id="brand-nike">
                                        <label class="form-check-label small fw-bold" for="brand-nike">
                                            Nike
                                        </label>
                                    </div>
                                    }
                                </div>
                            </div>
                            <!-- Add more brand options as needed -->
                        </div>
                        <!-- Filter by Size -->
                        <div class="mb-4">
                            <h5>Size</h5>
                            <div class="form-check card-scrollable">
                                <div class="row">
                                @for (int i = 0; i < 20; i++)
                                {
                                    <div class="col-sm-3 mx-2">
                                        <input class="form-check-input" type="checkbox" value="size-35" id="size-35">
                                        <label class="form-check-label small fw-bold" for="size-35">
                                            35
                                        </label>
                                    </div>
                                }
                                </div>
                            </div>
                            <!-- Add more size options as needed -->
                        </div>
                        <!-- Filter by Color -->
                        <div class="mb-4">
                            <h5>Màu sắc</h5>
                            <div class="form-check card-scrollable">
                                <div class="row">
                                @for (int i = 0; i < 20; i++)
                                {
                                    <div class="col-sm-3 mx-2">
                                        <input class="form-check-input" type="checkbox" value="color-red" id="color-red">
                                        <label class="form-check-label small fw-bold" for="color-red">
                                            Xanh
                                        </label>
                                    </div>
                                }
                                </div>
                            </div>
                            <!-- Add more color options as needed -->
                        </div>
                    </div>
                    <div class="col-lg-8">
                        <div class="container-xl px-4">
                            <hr class="mt-2" />
                            <!-- Product Show-->
                            @for (int i = 0; i < 4; i++)
                            {
                                <div class="row">
                                    @for (int j = 0; j < 4; j++)
                                    {
                                        <div class="col-lg-3 col-md-6 mb-4">
                                            <div class="card h-100">
                                                <a href="#">
                                                    <img class="card-img-top" src="/assets/img/products/adidas/nike-air-force-1-shadow-sail-tan-dv7449-100-600x600.jpg" alt="Sản phẩm">
                                                </a>
                                                <div class="card-body">
                                                    <div class="card-title">
                                                        <a href="#" class="text-decoration-none fw-bold" style="color: #446084">Onitsuka Tiger Mexico 66 Slip On – Midnight</a>
                                                    </div>
                                                    <strong class="text-orange">2.500.000 VND</strong>
                                                </div>
                                                <div class="card-footer d-flex justify-content-between align-items-center">
                                                    <button type="button" class="btn btn-sm btn-primary"><i class="fas fa-shopping-cart me-2"></i>Thêm vào giỏ hàng</button>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                            }
                        </div>
                    </div>
                </div>

            </main>
            @Html.Partial("_Footer")
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <script src="/js/scripts.js"></script>
    <script src="/js/jquery-3.6.0.min.js"></script>
    <script>
        const slider = document.querySelector(".slider");
        const images = document.querySelectorAll(".slider img");
        let counter = 1;
        const slideWidth = images[0].clientWidth;

        // Update initial position
        slider.style.transform = `translateX(${-slideWidth * counter}px)`;

        function nextSlide() {
            if (counter >= images.length - 1) {
                // Move to the cloned first image and reset counter
                slider.style.transition = "none";
                counter = 1;
                slider.style.transform = `translateX(${-slideWidth * counter}px)`;
            } else {
                slider.style.transition = "transform 0.5s ease-in-out";
                counter++;
                slider.style.transform = `translateX(${-slideWidth * counter}px)`;
            }
        }

        function prevSlide() {
            if (counter <= 0) {
                // Move to the cloned last image and reset counter
                slider.style.transition = "none";
                counter = images.length - 2;
                slider.style.transform = `translateX(${-slideWidth * counter}px)`;
            } else {
                slider.style.transition = "transform 0.5s ease-in-out";
                counter--;
                slider.style.transform = `translateX(${-slideWidth * counter}px)`;
            }
        }
    </script>
</body>
</html>
